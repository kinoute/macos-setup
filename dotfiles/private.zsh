# custom/private aliases for your own use
# I prefer to put them here instead of
# the main aliases.zsh because they are
# more related to our personal use.

alias wtsapp="cd ~/sites/whattheshot"
alias sshwts="ssh root@whattheshot.com -p 1987"
alias til="cd ~/sites/til && ./contribute.sh"
alias typora="open -a Typora"
alias tower="open -a Tower"

# suffix aliases
alias -s md=typora
alias -s git="git clone"
alias -s ipynb="open -a Jupyter\ Notebook\ Viewer"

